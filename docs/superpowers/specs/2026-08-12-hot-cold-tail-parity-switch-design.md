# 冷热出号尾数单双直接切换

## 范围

仅调整冷热出号中“尾数单双”的方案内容选择。每个可选位置最多保留一个属性。

## 交互

- 点击未选的“单”或“双”时，直接替换该位置已有的另一项。
- 再次点击当前已选项时，仍取消该项。
- 不调整其他按位大小单双玩法，也不调整过关玩法。

## 数据与校验

继续使用现有 `ranks`、`pool`、保存格式和注数计算；本次仅改变前端的单项选择状态转换。

## 验证

增加组件/视图逻辑测试：预先选“单”后点击“双”，结果仅保留“双”；再次点击“双”，结果为空。
