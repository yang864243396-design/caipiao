# 管理后台（`admin/`）

- 技术栈与用户端一致：**Vue 3 + TypeScript + Vite**（开发端口 **5174**）
- 从仓库根执行：`npm run dev:admin` 或本目录下 `cd admin && npm install && npm run dev`

## Mock 演示登录（仅本地 / 演示）

> **严禁** 将下列账号用于 **预发、生产或任何公网环境**。口令为弱密码，仅便于原型联调。

| 项目 | 值 |
|------|-----|
| 账号 | `admin` |
| 密码 | `admin123` |
