# 后端（`backend/`）

- 与 `client/`、`admin/` 通过 HTTP / WebSocket 等对接；**本目录为占位**，可按团队选型初始化例如：

  - **Go**（`go mod init` + `cmd/server`）  
  - **Node**（Nest / Fastify + `src`）  
  - 其它运行时  

- 建议后续在此补充：`openapi` 或接口文档、环境变量说明、与 `VITE_API_BASE_URL` 的对应关系（见用户端 `client/.env.example`）。
